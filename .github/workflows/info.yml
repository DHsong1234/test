name: Python package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
      - name: Display Python version
        run: pwd && git clone https://github.com/DHsong1234/test.git && find .  && echo "end job"
